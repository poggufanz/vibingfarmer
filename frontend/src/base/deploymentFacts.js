// A hardened deployment is activated only by publishing the verifier's complete, reviewed
// machine record here in a separate authorized change. The tracked Base Sepolia addresses in
// config.js remain useful for historical reads, but they are legacy and intentionally do not
// populate this execution-authority slot.
export const RECORDED_BASE_DEPLOYMENT = null
