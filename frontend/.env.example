# Frontend env template — copy to frontend/.env.local (gitignored) and fill in.
# Vite only exposes vars prefixed with VITE_ to the client bundle.

# Server-side AI key (NO VITE_ prefix) — read by the /api/ai proxy, never bundled to the client.
# OPTIONAL / host-funded. The app is BYOK-first: users paste their own Venice or
# DeepSeek key in Settings (per-tab sessionStorage). Set this ONLY if you want an
# out-of-the-box "host demo key" that anonymous visitors spend. LEAVE IT UNSET for a
# lockdown deploy — /api/ai then 503s and the app uses the deterministic fallback,
# so no stranger can drain your key.
DEEPSEEK_API_KEY=sk-your_deepseek_key

# A2A Coordination — orchestrator smart account
# The orchestrator key is an EPHEMERAL per-session key generated in-browser
# (see src/redelegation.js). No private-key env var here — a VITE_-prefixed key
# would be inlined into the public client bundle and readable by any visitor.
# Optional: custom Sepolia RPC for the orchestrator public client (defaults to https://rpc.sepolia.org)
VITE_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Tavily web search — live DeFi market context injected into the Venice AI strategy prompt.
# Server-side key (NO VITE_ prefix) — read by the /api/search proxy, never bundled to the client.
# OPTIONAL / host-funded, same BYOK model as DEEPSEEK_API_KEY: users can paste their
# own Tavily key in Settings (then the browser calls Tavily directly). If this is unset
# AND the user set no key, market search 503s/skips and strategy falls back to static
# knowledge only. Leave unset for a lockdown deploy.
TAVILY_API_KEY=tvly-...

# About section links (Settings → About). Optional — fall back to "#" if unset.
VITE_GITHUB_URL=https://github.com/your-org/vibing-farmer

# 1Shot Managed API — makes 1Shot relays REAL on Base Sepolia (gas-abstracted).
# Server-side only (NO VITE_ prefix) — read by the /api/relay proxy, never bundled.
# Get from app.1shotapi.com: API Keys (key+secret) and Organizations → Details (business id).
# Requires a Base Sepolia server wallet (Wallets tab) funded with testnet ETH — the
# proxy auto-provisions one on first call; fund the returned address for gas.
# If unset, deposits fall back to user-signed on-chain txs (real, but not gas-abstracted).
ONESHOT_KEY=your_1shot_api_key
ONESHOT_SECRET=your_1shot_api_secret
ONESHOT_BIZ_ID=your_1shot_business_id
# Server-controlled deposit target. The proxy NEVER trusts a client-supplied
# address — it registers + executes only against this contract. Set to the
# Base Sepolia AgentVaultDepositor address from your deploy (same as config.js).
AGENT_VAULT_DEPOSITOR_ADDRESS=0x...

# Security: allowed origin for /api/ai proxy
# Set to your Vercel deployment URL after deploy
ALLOWED_ORIGIN=https://your-app.vercel.app

